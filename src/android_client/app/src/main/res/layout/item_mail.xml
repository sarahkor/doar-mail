<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/card_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical">

        <!-- Unread Indicator -->
        <View
            android:id="@+id/unread_indicator"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:background="@drawable/circle_shape"
            android:backgroundTint="@color/unread_indicator"
            android:layout_marginEnd="8dp"
            android:visibility="gone" />



        <!-- Sender Avatar -->
        <ImageView
            android:id="@+id/iv_sender_avatar"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_gravity="center"
            android:background="@drawable/circle_shape"
            android:scaleType="centerCrop"
            android:layout_marginRight="10dp"
            android:src="@drawable/ic_account_circle"
            android:contentDescription="@string/avatar_desc"/>

        <!-- Mail Content -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Sender Name and Time -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginBottom="4dp">

                <TextView
                    android:id="@+id/tv_sender_name"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/sender_name"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="@color/mail_sender"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/tv_time"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/time"
                    android:textSize="12sp"
                    android:textColor="@color/mail_time"
                    android:layout_marginStart="8dp" />

            </LinearLayout>

            <!-- Subject -->
            <TextView
                android:id="@+id/tv_subject"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/subject"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@color/mail_subject"
                android:maxLines="1"
                android:ellipsize="end"
                android:layout_marginBottom="2dp" />

            <!-- Label Badge -->
            <TextView
                android:id="@+id/tv_label_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/label_name"
                android:textSize="12sp"
                android:textColor="@android:color/white"
                android:background="@drawable/bg_label_badge"
                android:paddingLeft="10dp"
                android:paddingRight="10dp"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:layout_marginBottom="2dp"
                android:layout_marginTop="2dp"
                android:visibility="gone" />

            <!-- Body Preview -->
            <TextView
                android:id="@+id/tv_body_preview"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/body_prev"
                android:textSize="14sp"
                android:textColor="@color/mail_preview"
                android:maxLines="2"
                android:ellipsize="end" />

        </LinearLayout>

        <!-- Action Icons Container -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginStart="8dp">

            <!-- Star Icon -->
            <ImageButton
                android:id="@+id/btn_star"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_star_outline"
                android:contentDescription="@string/star_icon"
                android:layout_marginEnd="4dp"
                app:tint="@color/text_secondary" />

            <!-- Label Icon -->
            <ImageButton
                android:id="@+id/btn_label_mail"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_label"
                android:contentDescription="@string/label_mail"
                android:layout_marginEnd="4dp"
                app:tint="@color/text_secondary" />


        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView> 